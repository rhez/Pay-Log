# NVS Pay Log Mockup

Open `index.html` directly in a browser (offline is fine) to view the static UI mockup. The page only references the local `style.css` file and does not depend on external assets.
